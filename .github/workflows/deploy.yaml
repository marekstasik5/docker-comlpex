name: Deploy MultiDocker
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t marek/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true marek/react-test npm test

      - run: docker build -t marek/multi-client ./client
      - run: docker build -t marek/multi-nginx ./nginx
      - run: docker build -t marek/multi-server ./server
      - run: docker build -t marek/multi-worker ./worker

      - run: docker push marek/multi-client
      - run: docker push marek/multi-nginx
      - run: docker push marek/multi-server
      - run: docker push marek/multi-worker
